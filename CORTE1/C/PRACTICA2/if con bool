#include <stdio.h>

int main () {
        int x = 20;
        int y = 18;

        bool isGreater = x > y;

        if (isGreater) {
                printf("x is grater than y");
}
        return 0;
}
